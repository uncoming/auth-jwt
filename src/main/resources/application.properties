spring.application.name=auth-jwt

# Configuraci�n del puerto del servidor
server.port=8080

# Configuraci�n de conexi�n a PostgreSQL Local
# spring.datasource.url=jdbc:postgresql://localhost:5433/devdb
# spring.datasource.username=devuser
# spring.datasource.password=devpass
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de conexión a PostgreSQL en Docker
spring.datasource.url=${AUTH_JWT_DATABASE_URL}
spring.datasource.username=${AUTH_JWT_DATABASE_USERNAME}
spring.datasource.password=${AUTH_JWT_DATABASE_PASSWORD}

# Configuraci�n de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
